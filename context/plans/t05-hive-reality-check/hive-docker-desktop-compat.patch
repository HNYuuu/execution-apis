diff --git a/internal/libdocker/container.go b/internal/libdocker/container.go
index 45960d85..f98839e1 100644
--- a/internal/libdocker/container.go
+++ b/internal/libdocker/container.go
@@ -174,6 +174,19 @@ func (b *ContainerBackend) StartContainer(ctx context.Context, containerID strin
 	}
 	info.IP = container.NetworkSettings.IPAddress
 	info.MAC = container.NetworkSettings.MacAddress
+	if info.IP == "" || info.MAC == "" {
+		for _, network := range container.NetworkSettings.Networks {
+			if info.IP == "" && network.IPAddress != "" {
+				info.IP = network.IPAddress
+			}
+			if info.MAC == "" && network.MacAddress != "" {
+				info.MAC = network.MacAddress
+			}
+			if info.IP != "" && info.MAC != "" {
+				break
+			}
+		}
+	}
 	return info, nil
 }
